spring:
  application:
    name: book-service
  cloud:
    consul:
      discovery:
        preferIpAddress: true
        tags: api
      host: consul
      config:
        enabled: true
        prefix: configuration
        defaultContext: apps
        profileSeparator: '::'